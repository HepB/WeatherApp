<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:itemTextAppearance">@style/TextAppearance</item>
        <item name="windowActionModeOverlay">true</item>
    </style>

    <style name="AppTheme.Text">
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

    <!--Тема для окрашивания текста в меню в черный цвет-->
    <style name="TextAppearance">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Text.TemperatureText">
        <item name="android:textSize">@dimen/main_temp_size</item>
        <item name="android:shadowDx">3</item>
        <item name="android:shadowDy">3</item>
        <item name="android:shadowRadius">5</item>
        <item name="android:shadowColor">@color/colorTemperature</item>
    </style>

    <style name="AppTheme.Text.ImportantText">
        <item name="android:textSize">@dimen/item_text_size</item>
    </style>

    <style name="AppTheme.WeatherTheme">
        <item name="android:background">@color/colorBackgroundNight</item>
    </style>

    <style name="AppTheme.WeatherItemTheme">
        <item name="android:background">@color/colorBackgroundDay</item>
    </style>

</resources>
